type Appointment {
    id: ID!
    patientId: ID!
    doctorId: ID!
    appointmentDate: String!
    reason: String!
    notes: String
    appointmentStatus: AppointmentStatus!
    specialty: String!
    location: String!
}

enum AppointmentStatus {
    SCHEDULED
    COMPLETED
    CANCELLED
}